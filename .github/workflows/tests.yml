name: Tests

on:
  pull_request:
  merge_group:
  push:
    branches:
      - main

jobs:
  linter:
    name: linter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26"
      - uses: actions/checkout@v6
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.11.4
      - name: Run go vet
        run: go vet
      - name: Yaml linter
        uses: ibiqlik/action-yamllint@v3
        with:
          config_file: .yamllint.yml

  integration_tests:
    runs-on: ubuntu-latest
    # Will not run if the event is a PR to bump-meilisearch-v* (so a pre-release PR)
    # Will still run for each push to bump-meilisearch-v*
    if: github.event_name != 'pull_request' || !startsWith(github.base_ref, 'bump-meilisearch-v')
    strategy:
      matrix:
        # go.mod requires Go 1.26.1; keep a single row until the next stable (1.27) ships.
        go: ["1.26"]
        include:
          - go: "1.26"
            tag: current

    name: integration-tests (go ${{ matrix.tag }} version)
    steps:
      - name: Set up Go ${{ matrix.go }}
        uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go }}

      - name: Check out code into the Go module directory
        uses: actions/checkout@v6

      - name: Get dependencies
        run: go mod download

      - name: Meilisearch setup (latest version) with Docker
        run: docker run -d -p 7700:7700 getmeili/meilisearch-enterprise:latest meilisearch --master-key=masterKey --no-analytics --experimental-allowed-ip-networks=any

      - name: Run unit tests
        run: |
          go test --race -v -gcflags=-l -coverprofile=unit_coverage.txt -covermode=atomic $(go list ./... | grep -v /integration)

      - name: Run integration tests
        run: |
          go test --race -v -gcflags=-l -coverprofile=integration_coverage.txt -covermode=atomic -coverpkg=./... ./integration

      - name: Merge coverage reports
        run: |
          go install github.com/ja7ad/gocovmerge/cmd/gocovmerge@v0.2.0
          gocovmerge unit_coverage.txt integration_coverage.txt > coverage.txt

      - name: Upload coverage report
        uses: codecov/codecov-action@v5
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
